package com.flip;

/**
 * Hello world!
 *
 */
public class App {
	public static void main(String[] args) {
		//System.out.println("Hello World!");

	/*	DatingService datingService = new DatingServiceImpl();

		List<String> interests = new ArrayList<String>();
		datingService.adduser("Male", interests, new Location(), 20);
		User newUser = datingService.getUser(1);

		Map<Integer, List<User>> usersMap = datingService.getMatches(1);

		for (Map.Entry<Integer, List<User>> entry : usersMap.entrySet()) {
			for (User tempUser : entry.getValue()) {
				boolean flag = true;
				if (true) {
					datingService.matchUser(newUser.getId(), tempUser.getId());
				}
			}
		}*/
	

		// Once we get all the usnr information

	}
}
